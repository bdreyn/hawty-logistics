{
  "name": "hawty-logistics",
  "version": "1.0.0",
  "private": true,
  "description": "Hawty Logistics marketing site — Eleventy + Tailwind + Sveltia CMS",
  "scripts": {
    "build:css": "tailwindcss -i ./src/assets/css/input.css -o ./src/assets/css/style.css --minify",
    "build:eleventy": "eleventy",
    "build": "npm run build:css && npm run build:eleventy",
    "watch:css": "tailwindcss -i ./src/assets/css/input.css -o ./src/assets/css/style.css --watch",
    "watch:eleventy": "eleventy --serve",
    "start": "npm run build:css && npm-run-all --parallel watch:css watch:eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^3.4.13"
  }
}
