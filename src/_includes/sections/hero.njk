<section id="hero" class="hero">
  <div class="hero-inner">
    <h1>{{ section.headline }}</h1>
    <p>{{ section.subtext }}</p>
    <a href="{{ section.ctaLink }}" class="btn btn-primary">{{ section.ctaLabel }}</a>
  </div>
</section>
